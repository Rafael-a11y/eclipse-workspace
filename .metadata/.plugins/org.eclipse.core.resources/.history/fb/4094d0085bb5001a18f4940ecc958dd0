package controle.ExerciciosEstruturaDeControle;

import java.util.Scanner;

public class Teste {public static void main(String[] args) {
	Scanner entrada = new Scanner(System.in);
	System.out.print("Entre com um número inteiro: ");
	int numero = entrada.nextInt();
	System.out.println("\nEntre com um segundo número inteiro: ");
	int numero1 = entrada.nextInt();
	entrada.close();
	if (numero + numero1 <= 100 && numero + numero1 >= 0 && numero + numero1 % 2 == 0) {
		System.out.print("\nO número é maior que 100");
	}
	else if (numero + numero1 == 18 || numero + numero1 == 26 || numero + numero1 == 50 || numero + numero1 ==70) {
		System.out.printf("\nO número %d é impar?", numero + numero1);
	}
	else {
		System.out.println("\nNumero inválido");
	}
}

}
